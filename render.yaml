services:
  - type: web
    name: resume-relevance-system
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: sh render-deploy.sh
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health